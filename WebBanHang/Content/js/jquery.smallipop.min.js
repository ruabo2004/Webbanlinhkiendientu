(function ($) {
    // Minimal smallipop stub to avoid errors if full plugin not available
    if (!$.fn.smallipop) {
        $.fn.smallipop = function (options) {
            // no-op: return jQuery collection for chaining
            return this;
        };
    }
})(jQuery);
